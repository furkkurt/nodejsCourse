/*
const int = setInterval(() => {     //global.setInterval
  console.log("in the interval");
}, 1000)

const greet = (name) => {
  setTimeout(() => {                //global.setTimeout
    console.log(`hello, ${name}`);
    clearInterval(int);
  }, 3000)
}
greet("furkan");
*/


console.log(__dirname);
console.log(__filename);
